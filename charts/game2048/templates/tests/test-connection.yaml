apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "game2048.fullname" . }}-test-connection"
  labels:
    {{- include "game2048.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "game2048.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
